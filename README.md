# MarsRoverDashBoard

> In root … "npm install"

cd public/server

> Run “Bower install”

NOTE : Depedning on access right you may have to install bower globally using the following command

> “npm install -g bower”

> Run "node default.js"
